# 화면정의서

각 화면은 목업 제작 시 이 정의를 기준으로 삼는다. 상태(State)는 Empty / Loading / Error / Success 4종을 기본으로 정의하고, 해당 없는 경우 생략한다.

---

## S-01. 로그인

| 항목 | 내용 |
| --- | --- |
| 목적 | GitHub OAuth 로그인 진입점 |
| 진입 경로 | 최초 방문(`/`), 세션 만료 후 재방문 |
| 구성 요소 | 서비스 로고/이름, 한 줄 소개 문구, "GitHub으로 로그인" 버튼 |
| 상태 | Loading: OAuth 리다이렉트 중 버튼 비활성 |
| 인터랙션 | 버튼 클릭 → `GET /auth/github` |
| 관련 API | `GET /auth/github`, `GET /auth/github/callback` |
| 비고 | 로그인 상태로 접근 시 `/dashboard`로 즉시 리다이렉트 |

---

## S-03. 대시보드

| 항목 | 내용 |
| --- | --- |
| 목적 | 로그인 후 기본 진입점. 활동/회고 현황을 한눈에 파악하고 오늘의 회고 작성으로 유도 |
| 진입 경로 | 로그인 직후, GNB "대시보드" |
| 구성 요소 | ① 히트맵(최근 N주 활동), ② 스트릭 배지, ③ "오늘의 회고 작성하기" CTA 버튼, ④ 액션포인트 목록(TODO·FAILED·ROUTINE), ⑤ 회고 요약 카드 리스트(최근 순) |
| 상태 | Empty: 회고/활동 이력이 전혀 없는 신규 사용자 → 기초 가이드 + 첫 회고 작성 유도 문구. Loading: 각 위젯 스켈레톤. Error: 위젯 단위 에러 표시(전체 화면 에러 아님) |
| 상태 태그 | 회고 요약 카드에 `분석중` 태그 — `summaryStatus = ANALYZING`일 때 노출, 완료 시 요약 텍스트로 교체 |
| 인터랙션 | 히트맵 셀 클릭 → S-09 팝업, 액션포인트 항목은 이 화면에서는 읽기 전용(수정은 S-04에서만), 회고 요약 카드 클릭 → S-08 상세 |
| 관련 API | `GET /dashboard` |
| 비고 | 분석중 카드는 폴링 또는 재진입 시 재조회로 갱신 (실시간 푸시는 범위 밖) |

---

## S-04. 회고 작성 — 진입 (액션포인트 리뷰 / 활동 확인)

| 항목 | 내용 |
| --- | --- |
| 목적 | 회고 작성 전 기존 액션포인트를 정리하고, 오늘의 회고 소재(Git 활동 또는 직접 입력)를 확보 |
| 진입 경로 | S-03 "오늘의 회고 작성하기" 클릭 |
| 구성 요소 | ① TODO·FAILED·ROUTINE 액션포인트 목록(항목별 "완료"/"실패"/"루틴"/"철회" 토글, 전이 제한 없음, 목록 없으면 섹션 자체 숨김), ② Git 활동 요약(있을 경우) 또는 직접 입력 텍스트 필드(없을 경우), ③ "다음" 버튼 |
| 상태 | Loading: 활동 수집/질문 생성 대기 중 "다음" 버튼 비활성 + 로딩 인디케이터. Empty: 활동 없음 → 직접 입력 UI로 자동 전환 |
| 인터랙션 | 액션포인트 토글 선택 → 로컬 상태 보관 후 "다음" 클릭 시 일괄 전송, "다음" 클릭 → 질문 생성 요청 → 성공 시 S-05로 전환 |
| 관련 API | `GET /action-points?status=TODO,FAILED,ROUTINE`, `GET /git-activities/today`, `PATCH /action-points/review`, `POST /retrospects/questions/generate` |
| 비고 | 액션포인트 섹션과 활동 확인 섹션은 한 화면 내 순차 노출(액션포인트 → 활동) 구성을 기본으로 하되, 목업 단계에서 2개 스텝으로 분리할지 결정 |

---

## S-05. 회고 작성 — 질문 답변 (단계형 폼)

| 항목 | 내용 |
| --- | --- |
| 목적 | 질문에 하나씩 답변하며 K/P/T를 실시간으로 누적 |
| 진입 경로 | S-04에서 질문 생성 완료 |
| 구성 요소 | ① 진행률 표시(예: "1/3"), ② 현재 질문 텍스트, ③ 답변 입력 필드(서술형), ④ "다음" 버튼, ⑤ (선택) 지금까지 누적된 K/P/T 미리보기 사이드 영역 |
| 상태 | Loading: 답변 제출 후 KPT 재구성 대기 중 "다음" 버튼 비활성. Error: 재구성 실패 시 해당 질문에 인라인 에러(전체 리다이렉트 아님 — 단, 재시도 실패가 반복되면 공통 에러 정책 적용) |
| 인터랙션 | 답변 입력 후 "다음" → `POST /retrospects/kpt/reconstruct` → 성공 시 다음 질문 또는(마지막 질문이면) S-06으로 전환 |
| 관련 API | `POST /retrospects/kpt/reconstruct` |
| 비고 | 챗봇형 대화 UI가 아니라 회원가입 마법사형 단일 질문 폼임을 목업에서 명확히 구분할 것 |

---

## S-06. 회고 작성 — 최종 확인

| 항목 | 내용 |
| --- | --- |
| 목적 | 누적된 K/P/T 전체를 검토하고 필요 시 직접 수정, 액션포인트를 작성(생성/수정/철회)한 뒤 저장 |
| 진입 경로 | S-05 마지막 질문 답변 완료 |
| 구성 요소 | ① Keep 목록(편집 가능), ② Problem 목록(편집 가능), ③ Try 목록(편집 가능, 최대 6개), ④ 항목 추가 버튼(K/P/T 각각), ⑤ 액션포인트 목록(진입 시 Try 기반 AI 후보 자동 생성 + S-04에서 이월된 기존 FAILED·ROUTINE 항목 포함 — 신규/직접추가 항목은 수정·삭제 자유, 이월 항목은 내용 수정 또는 철회만 가능), ⑥ 액션포인트 추가 버튼, ⑦ "저장" 버튼 |
| 상태 | Loading: 액션포인트 후보 생성 대기 중(진입 시), 저장 요청 중 버튼 비활성 |
| 인터랙션 | 각 항목 인라인 수정/삭제, "+" 로 항목 추가, "저장" 클릭 → `POST /retrospects` → 성공 시 S-03 복귀 |
| 관련 API | `POST /retrospects/action-points/generate` (진입 시), `POST /retrospects` (저장) |
| 비고 | 이 화면을 벗어나면(뒤로가기 등) 작성 내용은 소실됨 — 확인 다이얼로그 노출 여부는 목업 단계 결정 사항. 액션포인트 신규 생성 항목은 존댓말·표준어 보정 없이 입력 그대로 저장되며, 기존 항목과 중복 체크는 하지 않음 |

---

## S-07. 회고 기록 — 목록

| 항목 | 내용 |
| --- | --- |
| 목적 | 저장된 회고를 시간순으로 조회하고 검색 |
| 진입 경로 | GNB "회고 기록" |
| 구성 요소 | ① 검색 입력, ② 회고 카드 리스트(날짜, 요약 일부, 분석 상태 태그), ③ 페이지네이션 |
| 상태 | Empty: 저장된 회고 없음 → 첫 회고 작성 유도. Loading: 리스트 스켈레톤 |
| 인터랙션 | 카드 클릭 → S-08, 검색어 입력 → 목록 재조회 |
| 관련 API | `GET /retrospects?q=&page=` |
| 비고 | — |

---

## S-08. 회고 상세

| 항목 | 내용 |
| --- | --- |
| 목적 | 개별 회고의 전체 내용(질문/답변, K/P/T, AI 요약·인사이트) 열람 및 삭제 |
| 진입 경로 | S-07 카드 클릭, S-09 팝업 내 링크, S-03 요약 카드 클릭 |
| 구성 요소 | ① 회고 날짜, ② 질문/답변 전체, ③ K/P/T, ④ AI 요약/인사이트(또는 "분석중" 상태), ⑤ 해당 회고로 생성된 액션포인트 목록, ⑥ "삭제" 버튼 |
| 상태 | Loading: 상세 조회 중. 분석 미완료 시 요약/인사이트 영역에 "분석중" 표시 |
| 인터랙션 | "삭제" 클릭 → 확인 다이얼로그("액션포인트도 함께 삭제됩니다") → 확인 시 삭제 후 S-07 복귀 |
| 관련 API | `GET /retrospects/:id`, `DELETE /retrospects/:id` |
| 비고 | 수정 버튼 없음 (회고는 수정 불가 원칙) |

---

## S-09. 히트맵 상세 팝업

| 항목 | 내용 |
| --- | --- |
| 목적 | 특정 날짜의 활동과 회고 요약을 빠르게 미리보기 |
| 진입 경로 | S-03 히트맵 셀 클릭 |
| 구성 요소 | ① 날짜, ② 활동 요약(커밋/PR 수 등), ③ 회고 존재 시 요약 미리보기 + "상세 보기" 링크, ④ 회고 없을 시 안내 문구 |
| 상태 | Empty: 활동/회고 모두 없는 날짜 |
| 인터랙션 | "상세 보기" 클릭 → S-08, 배경 클릭/닫기 버튼 → 팝업 닫힘(S-03 유지) |
| 관련 API | `GET /git-activities/:date`, `GET /retrospects?date=` |
| 비고 | 모달 형태 (라우트 이동 없음) |

---

## 공통 에러 표시

전 화면 공통으로 별도 "에러 화면"을 두지 않는다. API 실패 시 토스트/얼럿 형태로 에러 메시지를 노출한 뒤 `/`로 리다이렉션한다 ([[03-user-flow#5-공통-에러-정책]] 참고). 구체적 UI 컴포넌트는 목업 단계에서 확정한다.

## 관련 문서

- [정보구조](./02-information-architecture.md)
- [유저플로우](./03-user-flow.md)
- [데이터/API 명세](./05-data-api-spec.md)
